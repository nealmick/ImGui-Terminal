.deepseek/

# Compiled object files
*.o
*.a

# St — upstream binary (built by `cd st && make`)
st/st
*.1

# Vim swap files
*.swp
*.swo
*~

# macOS
.DS_Store

# Generated headers
.config.h
st/config.h
config-imgui.h

# Patch backups
*.orig
*.diff

# ImGui runtime files
imgui.ini

# Build output (Makefile.imgui)
build/

# vcpkg — Windows users clone this into the repo root for the MSVC
# build (see README). 1+ GB after bootstrap; never tracked.
/vcpkg/

# Claude Code per-project state (settings, cached agent context, etc.)
.claude/

# Per-test latest run output — only expected.json is committed
tests/automation/*/*/actual.json